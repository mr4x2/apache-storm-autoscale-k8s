# GKE: no manual PV needed — mysql-pvc uses storageClassName: standard which triggers
# GKE's pd.csi.storage.gke.io driver to provision a Google Persistent Disk automatically.
# The hostPath PV that was here does not work on GKE (read-only node filesystem).
